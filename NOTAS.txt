
// nodemon server
npm run dev
=> http://localhost:3000/api

Comandos sequelize:
// crear modelos + migracion
sequelize model:create --name usuario --attributes nombre:string,email:string
// crear migracion
sequelize migration:generate --name create-usuario  
// crear seed
sequelize seed:generate --name seed-usuarios
// ejecutar migraciones
sequelize db:migrate 
// deshace la ultima migraciones // todas
sequelize db:migrate:undo //db:migrate:undo:all
// ejectuar seeds
sequelize db:seed:all  //db:seed:undo:all

Sequelize findAndCountAll() => Resultados paginados + numero total


Pendiente:
- Auth JWT => crear login
- Refresh Tokens
refresh token should be exchanged only with the authorization server to mitigate the risk of leakage, so it makes sense to send it only to the /refresh endpoint

TODO
Por que usuario_id en los 2 modelos en la relación?
Manejar solo 1 token por usuario

https://www.bezkoder.com/jwt-refresh-token-node-js/

https://dev.to/cyberwolves/jwt-authentication-with-access-tokens-refresh-tokens-in-node-js-5aa9
https://medium.com/@techsuneel99/jwt-authentication-in-nodejs-refresh-jwt-with-cookie-based-token-37348ff685bf


Notas:
- Envs: guardando variables/credenciales en .env (package dotenv)
  models > index.js carga los datos del entorno segun config.js que obtiene las variables de .env



Enlaces útiles:
https://sequelize.org/docs/v6/core-concepts/model-basics/